
      security:
        - bearerAuth: []
      tags:
        - 'Shopping Lists'
      summary: 'Listar listas de compra do usuário logado'
      consumes:
        - 'application/json'
      produces:
        - 'application/json'
      responses:
        200:
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: integer
                        name:
                          type: string
                        bought_at:
                          type: string
                          format: date-time
        401:
          $ref: '../../responses/401.yaml'
